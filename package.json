{
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "name": "vehicle_maintenance_project",
  "scripts": {
    "setup": "npm i && dfx canister create vehicle_maintenance_project_backend && dfx generate vehicle_maintenance_project_backend && dfx deploy",
    "build": "npm run build --workspaces --if-present",
    "prebuild": "npm run prebuild --workspaces --if-present",
    "pretest": "npm run prebuild --workspaces --if-present",
    "start": "npm start --workspaces --if-present",
    "test": "npm test --workspaces --if-present"
  },
  "type": "module",
  "workspaces": [
    "src/vehicle_maintenance_project_backend",
    "src/vehicle_maintenance_project_frontend"
  ],
  "devDependencies": {
    "@types/uuid": "^9.0.8"
  }
}
